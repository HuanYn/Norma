[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "norma-ai"
version = "0.1.0"
description = "Local AI worker for the Norma personal multimodal photo agent"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115,<1",
  "numpy>=2,<3",
  "opencv-python-headless>=4.10,<5",
  "pillow>=11,<13",
  "pydantic>=2.10,<3",
  "uvicorn[standard]>=0.34,<1",
]

[project.scripts]
norma = "ai.cli:main"

[project.optional-dependencies]
dev = [
  "httpx>=0.28,<1",
  "pytest>=8,<10",
]
selection = [
  "ortools>=9.12,<10",
]
multimodal = [
  "huggingface-hub>=0.34,<2",
  "open-clip-torch>=3.3,<4",
  "transformers>=4.57,<6",
]

[tool.pytest.ini_options]
testpaths = ["ai/tests"]
addopts = "-ra"

[tool.setuptools.packages.find]
include = ["ai", "ai.*"]
exclude = ["ai.tests", "ai.tests.*"]

[tool.setuptools.package-data]
ai = ["web_dist/index.html", "web_dist/assets/*"]
"ai.index" = ["openclip_xlm_roberta_manifest.json"]
"ai.rag" = ["qwen3_vl_2b_manifest.json"]
